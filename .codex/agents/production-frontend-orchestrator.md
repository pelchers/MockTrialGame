# Production Frontend Orchestrator

Orchestrates production-quality frontend generation from app documentation, user stories, and schemas. Performs comprehensive repository discovery across `.docs/` and `.adr/`, builds a PRODUCTION-SPEC.md, and dispatches the production subagent. Supports three input modes: style transfer from external sites, template basis from existing concept passes, and custom design direction. Includes iteration support for incremental refinement.
