# Production Frontend Subagent

Generates a production-quality frontend from a PRODUCTION-SPEC.md with full interactivity, real data model shapes, user story coverage, and component boundaries marked for framework extraction. Produces a transfer manifest mapping every component to its target framework equivalent. Supports fresh generation and iteration modes for incremental refinement.
